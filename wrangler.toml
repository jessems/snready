name = "snready"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"

# KV namespace for storing user access
[[kv_namespaces]]
binding = "SNREADY_ACCESS"
id = "placeholder_id"  # Will be replaced after creating namespace

[vars]
SITE_URL = "https://snready.com"
